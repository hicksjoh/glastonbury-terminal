# D1 — Fix FMP /stable endpoint error state

## Status
verified — ready for commit

## Timeline
- Started: 2026-04-20 21:05
- Last checkpoint: 2026-04-20 21:40
- Finished: 2026-04-20 21:40

## Verifier verdict: PASS
Evidence:
- `npx tsc --noEmit` → 0 errors
- Playwright smoke tests pass: `@smoke D1 — FMP sector performance` (2/2) against local dev
  - `GET /api/health reports fmp: ok` ✓
  - `GET /api/sectors returns a non-empty sectors array with numeric changes` ✓
- Live local response sample (2026-04-20):
  - Utilities −2.04%, Industrials +1.64%, Financial Services +1.20%, Communication Services −1.17%, Basic Materials +0.91%, Real Estate +0.87%, Consumer Cyclical −0.86%, Healthcare −0.67%, Consumer Defensive −0.45%, Energy …
- Data sourced from `/stable/sector-performance-snapshot?date=<recent trading day>` aggregated across NASDAQ/NYSE/AMEX exchanges.

## Diagnosis (run via scripts/diagnose-fmp.mjs)

The live health endpoint reported `fmp: error`. Live diagnostic found:

| Endpoint | Status |
|----------|--------|
| `/stable/quote?symbol=^VIX` | 200 ✓ |
| `/stable/quote?symbol=^GSPC` | 200 ✓ |
| `/stable/sector-performance` | 404 ✗ (wrong path — FMP migrated it) |
| `/api/v3/quote/^VIX` | **403 Legacy Endpoint** |
| `/api/v3/sector-performance` | **403 Legacy Endpoint** |
| `/api/v3/stock-screener` | **403 Legacy Endpoint** |
| `/stable/sector-performance-snapshot?date=2026-04-17` | 200 ✓ (new path) |
| `/stable/industry-performance-snapshot?date=2026-04-17` | 200 ✓ (bonus) |

**Root cause:** Wes is on FMP's new subscription tier. All `/api/v3/*` endpoints return 403 "Legacy Endpoint" as of Aug 31, 2025. The fix for D1 scope is the three `sector-performance` call sites. Broader `/api/v3` migration is follow-up tech debt.

## Scope for D1
- Create `src/lib/fmp-client.ts` with typed wrappers around `/stable` endpoints
- Patch 3 call sites that use sector-performance:
  1. `src/app/api/narrative/route.ts:53`
  2. `src/app/api/briefing/scheduled/route.ts:85`
  3. `src/app/api/sectors/route.ts:85`
- Add Playwright smoke test for `/api/sectors` and `/api/health` FMP status

## Out of scope (flagged as follow-up)
- `src/app/api/sectors/route.ts` also uses `/api/v3/quote/`, `/api/v3/stock-screener` — **all 403 on current tier**. These need a separate migration to `/stable/quote` and an alternative to the screener (use Polygon or Alpaca instead).
- `src/lib/market-intel.ts` has many `/stable` calls already but some helpers fall back to v3 patterns — audit separately.

## Files touched
- (in progress) src/lib/fmp-client.ts
- (in progress) src/app/api/briefing/scheduled/route.ts
- (in progress) src/app/api/sectors/route.ts
- (in progress) src/app/api/narrative/route.ts
- (in progress) e2e/D1-fmp-sectors.spec.ts
- scripts/diagnose-fmp.mjs (diagnostic, can be deleted post-merge or kept as tool)

## Open questions
- None

## Decisions made
- Use the most recent trading day (skip weekends) for sector-performance-snapshot's required `date` param
- Aggregate across exchanges (NASDAQ/NYSE/AMEX) since new endpoint returns one row per sector+exchange
- Keep the legacy `{ sector, changesPercentage }` shape so callers don't change
- Cache sector-performance result in memory 5 min
